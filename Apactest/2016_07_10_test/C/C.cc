#include <bits/stdc++.h>
using namespace std;

int main(int argc, char * argv[]) {
  int case_num = 0;
  cin >> case_num;
  for (int cs = 1; cs <= case_num; ++cs) {
    cout << "Case #" << cs << ": "  << endl;
  }
  return 0;
}
