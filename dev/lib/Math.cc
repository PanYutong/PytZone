#include <bits/stdc++.h>
using namespace std;

double change_root(double N) {
  return log10(N) / log10(2);
}

int main(int argc, char * argv[]) {
  double sum = 0;
  sum = (14375 * 13) * (1.08);
  cout << sum << endl;
  return 0;
}
