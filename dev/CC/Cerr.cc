#include <bits/stdc++.h>
using namespace std;

int main(int argc, char * argv[]) {

  if (true) {
    int i = 0;
    ++i;
    cout << i << endl;

    break;

    ++i;
    cout << i << endl;
  }

  return 0;
}
