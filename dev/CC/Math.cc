#include <bits/stdc++.h>
using namespace std;

double change_root(double N) {
  return log10(N) / log10(2);
}

int main(int argc, char * argv[]) {
  int i = 16;
  int j;
  j = i >> 1;
  cout << i << endl;
  return 0;
}
