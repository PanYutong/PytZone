#include <bits/stdc++.h>
using namespace std;

double change_root(double N) {
  return log10(N) / log10(2);
}

int main(int argc, char * argv[]) {
  char p = '+';
  printf("%d\n", atoi(p));
  return 0;
}
